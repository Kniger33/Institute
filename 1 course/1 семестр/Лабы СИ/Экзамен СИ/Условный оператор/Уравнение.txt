#include <stdio.h>
#include <math.h>

int main() {

    int a, b, c;
    scanf("%d%d", &a, &b);

    if (a == 0 && b == 0) {
        printf("INF");
    }
    else if (a == 0 && b != 0 || -b % a != 0) {
        printf("NO");
    }
    else if (-b % a == 0){
        printf("%d", -b / a);
    }



    return 0;
}
