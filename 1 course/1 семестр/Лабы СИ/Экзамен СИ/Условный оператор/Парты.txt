#include <stdio.h>

int main() {

    int a, b, c;
    scanf("%d%d%d", &a, &b, &c);

    int x = a / 2 + a % 2;
    int y = b / 2 + b % 2;
    int z = c / 2 + c % 2;

    printf("%d", x + y + z);

    return 0;
}
 