#include <stdio.h>
#include <math.h>

int main() {

    int a, b, c;
    scanf("%d", &a);

    if ((a + 4) % 4 == 0 && a != 0 || a == 1) {
        printf("YES");
    }
    else {
        printf("NO");
    }
    return 0;
}
