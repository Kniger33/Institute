#include <stdio.h>
#include <math.h>

int main() {

    int a, b, c, d;
    scanf("%d%d%d%d", &a, &b, &c, &d);

    if (fabs(a - c) == fabs(b - d)) {
        printf("YES");
    }
    else {
        printf("NO");
    }

    return 0;
}
