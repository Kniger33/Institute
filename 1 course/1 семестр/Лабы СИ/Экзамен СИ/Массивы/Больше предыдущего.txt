#include <stdio.h>
     
int main () {
    int N, count = 0;
    scanf("%d", &N);
    int mas [N];
     
    for (int i = 0; i < N; i++){
        scanf("%d", &mas[i]);
    }
     
    for (int i = 1; i < N; i++){
        if (mas[i] > mas[i - 1]) {
            printf("%d ", mas[i]);
        }
    }
     
    
    
    return 0;
}
