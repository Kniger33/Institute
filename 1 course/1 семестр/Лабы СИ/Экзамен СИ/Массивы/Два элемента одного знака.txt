#include <stdio.h>
     
int main () {
    int N, x, y;
    scanf("%d", &N);
    int mas [N];
    
    for (int i = 0; i < N; i++){
        scanf("%d", &mas[i]);
    }
     
    for (int i = 0; i < N - 1; i++){
        if (mas[i] > 0 && mas[i + 1] > 0 || mas[i] < 0 && mas[i + 1] < 0) {
            if (mas[i] > mas[i + 1]) {
                 printf("%d %d", mas[i + 1], mas[i]);
            }
            else {
                printf("%d %d", mas[i], mas[i + 1]);
            }    
            //check = true;
            /*x = mas[i];
            y = mas[i + 1];*/
            break;
        }
        
    }
     
    return 0;
}

