#include stdio.h
#include stdlib.h


int main () {

    int count = 0, i;
    char str[1000];

    gets(str);

    for (i = 0; str[i]; i++){
        if (str[i] == ' '){
            count++;
        }
    }
    printf(%d, count + 1);

    return 0;
}
