#include <stdio.h>

int main() {
    int N, i;
    scanf ("%d", &N);
    
    int mas[N + 1];
    mas[0] = 0;
    mas[1] = 1;
    for (i = 2; i < (N + 1); i++){
        mas[i] = mas[i - 1] + mas[i -2];
    }
    printf("%d", mas[N]);
    return 0;
}