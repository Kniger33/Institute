#include <stdio.h> 
#include <stdlib.h> 

int main() { 
    
    int a1 = 1, a2 = 0, a3 = 0, current_max = 0, min_range = 100000, last_max = 1; 
    
    scanf("%d%d", &a1, &a2); 
    
    int i = 0, x = 1; 
    
    while (a1 != 0) { 
        i++; 
        a3 = a2; 
        a2 = a1; 
        scanf("%d", &a1); 
        
        if ((i > 2) && (a2 > a1) && (a2 > a3) && (a1 != 0)) { 
            current_max = last_max; 
            last_max = i; 
                if ((last_max > 0) && (current_max > 0) && (last_max - current_max < min_range)) { 
                    min_range = last_max - current_max; 
                } 
        } 
    } 
    if (min_range == 100000) { 
        min_range = 0; 
    } 
    printf("%d\n", min_range); 
    return 0; 
}