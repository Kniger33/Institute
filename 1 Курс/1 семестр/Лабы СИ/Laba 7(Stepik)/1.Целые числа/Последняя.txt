#include <stdio.h>

int main() {
    int H, A, B;
    scanf("%d%d%d", &H, &A, &B);
    int s = 0, days = 0;
    while (s != H){
        days++;
        s+= A;
        if (s >= H) {
            break;
        }
        else s -= B;
    }
    
    printf("%d", days);
    return 0;
}