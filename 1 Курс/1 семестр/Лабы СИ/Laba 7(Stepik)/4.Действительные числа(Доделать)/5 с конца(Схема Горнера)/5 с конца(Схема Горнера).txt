#include <stdio.h>
#include <stdlib.h>

int main() {

    int N, i, num1 = 1, num2;
    double x, y, sum;

    scanf("%d", &N);
    scanf("%lf", &x);


    for (i = 0; i < N + 1; i++){
        scanf("%lf", &y);
        num1 = y;
        sum *= x;
        sum += y;
    }


    printf("%0.6f", sum);

    return 0;
}
