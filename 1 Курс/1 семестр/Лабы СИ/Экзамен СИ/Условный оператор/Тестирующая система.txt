#include <stdio.h>
#include <math.h>


int main() {

    int a, b;
    scanf("%d%d", &a, &b);

    if ((a != 1 && b == 1) || (a == 1 && b != 1)) {
        printf("NO");
    }
    else printf("YES");

    return 0;
}
